{% for item in items %}
=== "{{ item.method }} {{ item.name }}"

    {{ item.description | indent(4, False) }}

    {% if item.parameters %}
    #### Parameters

    | Parameter | Type | Required | Description | Example |
    | --- | --- | --- | --- | --- |
    {% for param in item.parameters %}| `{{ param.name }}` | `{{ param.type }}` | {{ "Yes" if param.required else "No" }} | {{ param.description }} | `{{ param.example }}` |
    {% endfor %}
    {% endif %}

    {% if item.request_body %}
    #### Request Body
    
    ```json
{{ item.request_body | indent(4, True) }}
    ```
    {% endif %}

    {% if item.snippets %}
    #### Request Examples

    === "cURL"

        ```bash
{{ item.snippets.curl | indent(8, True) }}
        ```

    === "Python (Requests)"

        ```python
{{ item.snippets.python | indent(8, True) }}
        ```

    === "JavaScript (Octokit)"

        ```javascript
{{ item.snippets.javascript | indent(8, True) }}
        ```
    {% endif %}

    #### Response Example
    
    ```json
{{ item.response_body | indent(4, True) }}
    ```

    {% if item.notes %}
    > [!NOTE]
    > {{ item.notes | indent(4, False) }}
    {% endif %}

{% endfor %}
